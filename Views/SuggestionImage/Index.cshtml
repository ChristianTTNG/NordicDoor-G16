@model IEnumerable<NordicDoors.Models.SuggestionImage>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SugState)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuggestionId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SugState)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuggestionId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuggestionImageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuggestionImageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuggestionImageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
