@model IEnumerable<NordicDoors.Models.TeamMembership>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeamID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeamID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeamMembershipID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeamMembershipID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeamMembershipID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
